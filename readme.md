# Simple Interest Calculator

This project is a simple interest calculator written in Bash.

The calculator asks the user to enter:

* Principal amount
* Rate of interest
* Time period

It then calculates simple interest using the formula:

**Simple Interest = (Principal × Rate × Time) / 100**

The project demonstrates basic Bash scripting, user input, variables, and arithmetic calculations.
